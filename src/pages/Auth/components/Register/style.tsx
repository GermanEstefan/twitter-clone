import styled from "styled-components";

export const RegisterStyled = styled.div`
    display:flex;
    flex-direction:column;
    align-items:center;
    padding: 0px 15px;
    max-width:320px;
`;

export const RegisterMessageStyled = styled.h1`
    font-size:1.4rem;
    margin-bottom:30px;
`;

export const PoliticsParagraphStyled = styled.p`
    font-size:.8rem;
    margin-top:20px;
`;

export const BStyled = styled.b`
    color:var(--primary);
    font-weight:lighter;
`;